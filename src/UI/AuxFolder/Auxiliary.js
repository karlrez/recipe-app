/**
Auxiliary class only outputs child elements from props given. Using this
instead of adding a wrapping element to multiple components.
**/
const aux = (props) => props.children;

export default aux;
